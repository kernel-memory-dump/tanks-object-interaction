export const logEvent = (message: string) => {
    console.log(message);
}

export const clearAndPrintBanner = () => {
    console.clear();
    console.log(`====Object Interaction assignemnt - TANKS====`);
}